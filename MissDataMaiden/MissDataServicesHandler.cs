using MediatR;
using MissBot.Handlers;

namespace MissDataMaiden
{
    //internal class MissDataServicesHandler : BotServicesHandler<MissDataMaid>
    //{
    //    IMediator mm;
    //    public MissDataServicesHandler(IMediator mediator)
    //        => mm = mediator;
    //    public override async Task ExecuteCommand<TCommand>(TCommand command)
    //    {
    //        await mm.Send(new MissDataMaid.HandleBotCommandRequest<TCommand>(command));
    //    }
    //}
}
